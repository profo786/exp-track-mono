name: Deploy

on:
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  noop:
    name: "No deploy jobs configured"
    runs-on: ubuntu-latest
    steps:
      - name: No-op
        run: echo "Deploy workflow intentionally empty."
